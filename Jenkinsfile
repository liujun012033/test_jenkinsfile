@Library('shared_jenkins') _
import com.bne.utils.Utils

pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                script {
                    Utils.printHello(this)
                }
            }
        }
    }
}
